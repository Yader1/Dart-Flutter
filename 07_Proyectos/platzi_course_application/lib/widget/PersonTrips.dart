import 'package:flutter/material.dart';

class PersonTrips extends StatelessWidget {
  const PersonTrips({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}